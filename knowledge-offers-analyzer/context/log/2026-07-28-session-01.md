---
title: Session log 2026-07-28 (01)
type: context
updated: 2026-07-28
---

# Session log - 2026-07-28 (01)

## Goal

Harden the knowledge-vault second brain so its protocol is portable across agent runtimes and
its structural invariants are checked automatically.

## Changes

1. Added `AGENTS.md` as the portable agent entry point. It points to `CLAUDE.md` rather than
   duplicating policy.
2. Added `npm run vault:check` (`scripts/check-vault.js`). It validates required frontmatter and
   malformed wikilinks; unresolved future-note links remain warnings because the vault protocol
   allows them as TODO markers.
3. Added the GitHub Actions quality workflow: vault check, typecheck, lint, unit/integration
   tests, and API contract tests on pushes to `main` and pull requests.
4. Added canonical backlog notes [[SPEC-005]] and [[SPEC-006]], linked them from [[specs/README]],
   and repaired malformed wikilink aliases plus the non-note `[[B20]]` reference.
5. Clarified the RTK fallback for Windows-only runtimes in `CLAUDE.md` and
   [[environment-setup]].
6. Moved `isolatedModules` to `tsconfig.json`, removing the `ts-jest` deprecation warning from
   both test configurations.

## Verification

- `npm run vault:check`
- `npm run typecheck`
- `npm run lint`
- `npm test -- --runInBand`
- `npm run test:contract -- --runInBand`

## Vault sync

- [[vault-protocol]] and [[00-INDEX]] record the portable entry point and automated vault gate.
- [[specs/README]] now exposes every backlog-level spec as a navigable vault node.
- No product decision changed; no supersession sweep required.
