---
title: Session log — 2026-07-18
type: context
updated: 2026-07-18
---

# 2026-07-18 — vision shift: operator-profit ranking (ADR-0006 + spec 003)

## Context

Operator proposed a product-vector change (P0–P15): a *deal* is no longer "priced below fair
market value" but "**high probability of operator profit on resale**" — the product becomes an
асистент перекупа that ranks by expected profitability. Scope agreed: vault + ADR + spec 003
now; implementation later. Roadmap to be **corrected against the real system state**, not
copied verbatim (several proposed items already exist).

## Done

- **ADR-0006** (`0006-operator-profit-vision.md`, Accepted) — new vision; composite Total Deal
  Score with binding invariants (price dominant, graceful degradation, hard disqualifiers clamp
  ≤ 0, ParameterSet-tunable, one-message explainable); positives-may-uplift (supersedes B24's
  "never inflate"); segment mileage norms; operator-thinking test; not-an-appraiser; ML stays
  deferred. Registered in decisions/README.
- **Constitution 1.0.2 → 1.1.0** — preamble reframed to operator-profit ranking; workflow gate
  #5 (operator-value test). Amendment via ADR-0006 per governance.
- **Spec 003** `specs/003-composite-deal-score/` (spec + plan + tasks, Draft): Phase F
  behavior-preserving skeleton (neutral modifiers, SC-001 bit-identical guard, 0–100
  presentation) → US1 liquidity → US2 repair-risk → US3 negotiation+seller → US4 positive
  uplift → US5 segment mileage norms → rollout (threshold re-validation — score shape changes).
  Heuristic tables as versioned `config/heuristics/*.json`, bounds in ParameterSet, zero extra
  API budget (all factors from already-fetched `/info` fields + stored description).
- **Roadmap correction (P→reality)** — table in [[backlog]] (🟠 epic): already built —
  condition (C1–C3), listing-level red-flags, `/why` (B22), threshold auto-calibration (E3);
  absorbed — B24 → US4 (scope upgraded), P5+P8 merged into US2; raised — B25 (Should-Have,
  + market-demand later); unchanged — no-ML verdict.

## Supersession sweep (ADR-0006)

Updated in the same task: [[goals]] (vision + stack line + later-scope), 00-INDEX one-liner,
[[profitability-definition]] (status Accepted; reframed intro; deal score = **price core**),
[[profitability-methods-coverage]] (§1 definition, §2 row 7, §4 re-cut around spec 003),
[[glossary]] (Opportunity redefined; + Total Deal Score, Liquidity score, Repair-risk,
Negotiation signal, Positive signal), [[how-it-works]] (business intro: асистент перекупа),
backlog (B24 absorbed/superseded clause, B25 raised), specs/README (+003 row), constitution.
Old "positives never inflate" now survives only in historical logs (not curated notes).

## Open / next

- Implement spec 003 starting at Phase F (blocked on nothing; B23 persistence should land with
  Phase F).
- After S1/S2 and S4: threshold re-validation (`/report` + calibration on the new score
  distribution) — old `minDealScore` values are not comparable across the score-shape change.
- Curation of the first heuristic tables (liquidity tiers, repair-risk patterns, mileage norms,
  lexicons) — operator input welcome; tables are config, mistakes are cheap to fix.

## Spec 003 — Phase F implemented (composite skeleton, behavior-identical)

Implemented the foundational, unblocked slice of spec 003 (T001–T003) directly (not delegated).
The score is now `priceCore × Π(factor modifiers)` — but **no factors ship yet**, so the product
is 1.0 and behavior is bit-identical (SC-001).

- `valuation/factors/factor.ts` (new, pure): `FactorScore`, `composeFactors` (dampeners apply in
  full; combined **uplift** clamped to `upliftCap` → price dominance), `toTotal100` (score→0–100,
  at-market=50), `toSubScore100`.
- `ValuationResult` += `priceCore`, `factors[]`, `total100`; `computeValuation` renamed the old
  `score` to `priceCore`, composes (empty) factors, and gates `isOpportunity` additionally on
  `priceCore > 0` (redundant while `minScore > 0`, so no behavior change).
- `ScoringParams` (jsonb) += optional `factorBounds` / `upliftCap` / `heuristicTableHashes`;
  `buildSeedParams` seeds them neutral, absent → `DEFAULT_UPLIFT_CAP` 1.25 → **existing v1 rows
  valid, no migration**.
- Presentation: `📊 Загальний бал: N/100` added to `/why`, opportunity + assessment alerts; factor
  lines render when factors exist (none now). Additive → formatter `toContain` tests hold.
- T004 (heuristic loader) and T005 (persist factors / B23) **deferred** — nothing to load/persist
  until a real factor ships.

Verify: `./tools/rtk npx tsc` clean; `factor.spec` 7/7. valuation/alert-format suites hit the known
sandbox-jest degradation (typeorm cold require) → full run deferred to the dev machine; SC-001 holds
by construction (empty Π returns `priceCore` exactly; all valuation.spec numerics depend only on
`score`). Next unblocked slice: Phase 1 (US1 liquidity + US2 repair-risk), each behind the
`ParameterSet` with conservative bounds, then threshold re-validation (T050).

## Spec 003 — Phase 1 implemented (US1 liquidity + US2 repair-risk)

First two live factors, both **off by default** (gated by their `ParameterSet` bound + heuristic
table → neutral seed keeps SC-001) — enabling is an explicit `ParameterSet` activation carrying
`PHASE1_FACTOR_BOUNDS`, followed by threshold re-validation (S6/T050).

- **Loader** `factors/tables.ts` — `HeuristicTablesService` loads/validates `config/heuristics/*.json`
  at boot, tolerant of a bad/missing file (factor off, never crashes scoring), content-hashes tables.
- **US1 liquidity** — `config/heuristics/liquidity-tiers.json` (A–D by make/model + make fallback) +
  pure `factors/liquidity.ts` (tier→modifier within bounds; unlisted→neutral-with-reason). 7/7.
- **US2 repair-risk** — `config/heuristics/repair-risk.json` (model/make tiers + gearbox/engine/fuel/age
  patterns: DSG/CVT/air-susp/aged turbo-diesel/big-premium → HIGH; Toyota/Honda/Mazda… → LOW) + pure
  `factors/repair-risk.ts` (HIGH→lower bound, LOW→upper, MEDIUM→neutral; model/make tier precedence,
  then patterns, then neutral-with-reason). 10/10. `/info` `gearboxName`/`fuelName`/`modificationName`
  verified live and mapped in `AutoRiaSource` → `ListingDetail`.
- **Wiring** — `computeValuation(input, params, tables)` runs both factors (each gated by its bound);
  `ValuationInput` += make/model/gearbox/fuel/engine; `ValuationService` injects the loader;
  poll + query pass the new fields. Integration: liquid-vs-illiquid + reliable-vs-risky ordering,
  price-dominance guard.

Verify: `./tools/rtk npx tsc` clean; `liquidity` 7/7, `repair-risk` 10/10, `factor` 7/7. The
typeorm-importing integration suite is deferred to the dev machine (sandbox jest degraded); its two
new ordering cases are hand-verified (both factors move the score monotonically, priceCore unchanged).
Note: much of US2 (factor, table, unit tests, port+adapter mapping) was authored by the operator; I
reviewed it, wired the poll/query call sites, added the integration case, and ran the sweep.

Next: **Phase 2 (US3 seller-motivation + seller-type)** — reuses the condition-parser approach for a
negotiation lexicon; or **S6 threshold re-validation** once a factor is switched on in a real
`ParameterSet`.

## Related

- [[backlog]] · [[0006-operator-profit-vision|ADR-0006]] · [[profitability-definition]] ·
  [[profitability-methods-coverage]]
