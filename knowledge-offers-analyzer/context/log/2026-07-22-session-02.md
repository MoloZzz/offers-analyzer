---
title: Session log 2026-07-22 (02)
type: context
updated: 2026-07-22
---

# Session log — 2026-07-22 (02)

## Goal
Capture the operator's "оцінка вигідності" backlog doc (SPEC-004..008, ADR for rate limiting,
FIX-003.1, CHANGE-002.1/3.2/3.3) into the vault per `CLAUDE.md`'s write protocol — this was a pure
knowledge-base update, no code changed.

## Done
1. **decisions/0009-monthly-rate-limit-pool.md** — new ADR. Note: the doc's own numbering called
   this "ADR-0007", but ADR-0007 is already taken (structured logging, 2026-07-19) — renumbered to
   **ADR-0009** (next free slot after 0008). Indexed in `decisions/README.md`.
2. **context/backlog.md** — new epic "Оцінка вигідності v2" with SPEC-004 (survivorship correction
   `k`), SPEC-005 (tiered re-check), SPEC-006 (money output `Z`/ROI), SPEC-007 (outcome labels
   beyond 👍/👎), SPEC-008 (cohort drift), FIX-003.1, CHANGE-002.1/3.2/3.3, the "what NOT to do"
   list, and the execution order table. Linked to ADR-0009/0006/0005.
3. **domain/glossary.md** — 5 new terms: survivorship correction (`k`), Days on Market (DOM),
   re-check tier (ярус), cohort drift, Z/ROI.
4. **research/profitability-definition.md** — added the survivorship-bias hypothesis as the
   leading pitfall entry under "Known pitfalls of the average-price anchor", linked to SPEC-004.
5. **specs/README.md** — new "Backlog-level specs (pre-Spec-Kit)" table so the specs MOC surfaces
   these before they're formally run through `/speckit-specify`.

## Decisions / Notes
- Central hypothesis worth flagging for future agents: `fair_value` may be inflated 8–15% by
  survivorship bias (overpriced listings linger in the "active" sample, fairly-priced ones sell
  and vanish). SPEC-004 measures this for free via the existing id-list diff. If the measured `k`
  comes back ≥0.97, the hypothesis is falsified — look at cohort composition or mileage correction
  instead.
- Treated ADR-0009 as **Accepted** (not "Proposed") — consistent with how ADR-0005 was recorded:
  the operator has already decided the policy; what's pending is implementation, not agreement.
- Did not touch `goals.md` or `0006-operator-profit-vision.md` — this backlog extends spec
  002/003 execution detail, it doesn't change the vision ADR-0006 already states. If SPEC-006
  (money output) is later adopted as the primary gate instead of the score, that *would* warrant
  a goals.md/ADR-0006 update — flagged as a future trigger, not done now.
- No `.specify/` spec directories created for SPEC-004..008 — they're backlog-level, not yet run
  through Spec Kit. Per `CLAUDE.md` §2, promote to a formal spec before implementation.

## Next
Per the backlog's own execution order: FIX-003.1 first (15 min, checks whether spec 003's factor
bounds are actually live in prod), then SPEC-004 US4.1/4.2 (data collection, zero API cost) and
SPEC-007 US7.1/7.2 (outcome fields) in parallel — both are unblocked and cost nothing, and every
day of delay on data collection pushes the rest of the roadmap back by the same amount.

## Related
- [[backlog]] · [[0009-monthly-rate-limit-pool|ADR-0009]] · [[profitability-definition]] ·
  [[glossary]] · [[specs/README]]
